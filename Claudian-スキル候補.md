---
tags: [claudian, skill-backlog, self-improving]
created: 2026-04-25
last_updated: 2026-04-29
---

# 🪴 Claudian スキル候補（Skill Backlog）

> **このファイルの役割**
> - Claudianが「同じ作業パターンを3回以上見た」「ユーザーが繰り返し似た指示を出している」と気づいたとき、新スキル候補をここに積む
> - すぐ実装するわけではない。アイデアを失わないための置き場
> - `/reflect` スキルが自動で追記する。手動で書き込んでもよい
> - 一定数貯まったら、ユーザーと「これ実装する？」を相談する

---

## 📌 ステータス凡例

- 🟡 **観察中**: パターン検出済み。3回未満。様子見
- 🟢 **候補確定**: 3回以上検出。実装してよさそう
- 🔵 **実装決定**: ユーザー承認済み。次のセッションで作る
- ⚪ **見送り**: ユーザー判断で実装しない（理由を記載）
- ✅ **実装済み**: スキル化された（実体ファイルへのリンクを追記）

---

## 🟢 候補確定（≥3回検出）

> ※ 現在エントリなし

---

## 🟡 観察中（1〜2回検出）

### `/membership`（仮称）— note月額メンバーシップの月次配信原稿生成

- **発生**: 2026-05-03、ユーザー言及（メンバーシップ準備着手の文脈）
- **観察回数**: 1回
- **背景**:
  - W18振り返りで「AIニュース系は読まれてない」が確定 → メンバーシップ中身を「批評型ロング + ルーチンB集 + 失敗談 + 実装ツール」へ転換
  - W20（5/11〜）から本格準備、W22末でGo/No-Go判定、最短でW23（6/1）開設
  - 開設後は **月次配信原稿4〜6本** を毎月安定生産する必要が出る → 手作業だと負担大
- **想定されるフロー**:
  ```
  月初:
    /membership → 当月配信予定の原稿セット一括生成:
      ├→ 批評型ロング解説 ×2（4/26リプの拡張型 / ルーチンB週次集計から派生）
      ├→ 失敗談 ×1（700円→3万円系を当月切り口で）
      ├→ 実装ツール紹介 ×1（戦略①ミニツールから連動）
      └→ ルーチンB批評リプ集 ×4週分（週次配信用）
    各原稿は my-clone人格で書き、_メンバーシップ/配信原稿/ に下書き
    _運営台帳.base に予定行を自動追加
  ```
- **設計選択肢**:
  - 案A: 月初一括生成（ネタの一貫性キープしやすいが、月の途中の時事を反映しにくい）
  - 案B: 週次生成（時事性◎、月単位の編集方針がブレやすい）
  - 案C: ハイブリッド（月初に骨子だけ、週次で時事ネタ肉付け）← 推奨
- **実装上の懸念**:
  - 月4〜6本 × 各3〜5KB は LLM 出力量が中規模。1回呼び出しで全部生成は品質下がる → **1原稿1呼び出し**が現実的
  - my-clone人格データの参照を強制（実績語りNG・観察試行錯誤ベース）
  - W18勝ち型（批評＋同業者体験＋軽い否定＋絵文字1つ＋断定しない語尾）のテンプレ化を内蔵
- **依存関係**:
  - W22末のローンチ判定がGoの場合のみ実装着手
  - [[SNS運用/note/_メンバーシップ準備ロードマップ.md]] の進行と連動
  - `_運営台帳.base` の作成（W20 Day2予定）が前提
- **実装優先度**: 中（W22 Go判定後に🟢→🔵昇格判定）
- **次の観察ポイント**: W20 Day5（5/15）に🟡→🟢格上げの是非を判断（ロードマップに明記済み）

---

### `/weekly-generate-posts`（仮称）— 翌週1週間分の日次投稿ファイル一括生成

- **発生**: 2026-04-29、ユーザー言及（n8n × Bluesky 自動投稿の入力データ問題から派生）
- **観察回数**: 1回
- **背景**:
  - ユーザーは「`/weekly-analytics` 後に翌週1週間分の day*.md / Threads-day*.md が自動生成される」と想定していたが、現実は分析レポート1本のみで未実装
  - 現状の day*.md / Threads-day*.md は実質**毎日手動**で作成されている
  - n8n × Bluesky 自動投稿（[[2026-04-29-sns-automation-implementation-plan]]）の入力データ問題（**「最新ファイル探し」がアーカイブ運用と相性悪い**）の本質的解決にも繋がる
- **想定されるフロー**:
  ```
  日曜夜:
    /weekly-analytics → WNN分析レポート + アクション3つ（既存）
       ↓
    新スキル → 翌週7日分を一括生成:
      ├→ SNS運用/post/day{N+1〜N+7}.md（X日次）
      ├→ SNS運用/threads/Threads-day{N+1〜N+7}.md（Threads日次）
      └→ SNS運用/bluesky/Bluesky-day{N+1〜N+7}.md（Bluesky日次・新規）★
  ```
- **設計選択肢**:
  - 案A: `/weekly-analytics` 拡張（1コマンド完結だが責務肥大）
  - 案B: 新規スキル `/weekly-generate-posts` 新設（推奨：責務分離）
- **実装上の懸念**:
  - 7日 × 3媒体 = 21ファイル一括生成は LLM 出力量が膨大。**3日分ずつ × 3回の分割実行**を組み込むと品質キープしやすい
  - 各日の連投シリーズ・Day Oneプロモ・ニュース連動を踏まえた本文生成 = 1ファイルずつ思考が必要
- **依存関係**:
  - n8n × Bluesky Phase 1 完走後がベター（[[2026-04-29-sns-automation-implementation-plan]]）
  - Bluesky アーカイブ受領後のシナリオB検討（[[2026-04-29-birdclaw-x-archive-tool]]）と前後する
- **実装優先度**: 中〜高（n8n自動投稿の入力データ問題を本質解決するため、Phase 1 安定後の最有力候補）
- **次の観察ポイント**: ユーザーが類似の運用要望を再度言及するか、Phase 1 安定運用後に「やはり週次生成が欲しい」と再認識するか

---

## ✅ 実装済みアーカイブ

> 過去にここから昇格したスキルを記録（スキル一覧との二重管理にしない、ここはあくまで「発生→実装」の系譜のみ）

### `/news-thread` — AIニュースv2型5投稿スレッド生成

- **発生 → 実装**: 2026-04-27（観察→候補化を経ず、ユーザー明示依頼で直接🔵→✅）
- **トリガー**: ユーザーが [[SNS運用/analytics/news_thread_v2.md]] の生成方法を質問。テンプレが完成済みかつ火・金週2回モデル（W18: 5/1初試行 → W19以降は火金で恒常運用）で再現性が高いと判断
- **既存スキルとの関係**: `/thread`（note→8投稿）と思想が別、`/re-daily` Step 9 とも独立。重複なし
- **実装**: [[.claude/commands/news-thread.md]] — 9ステップ、入力4パターン、ソース候補3ディレクトリ（`調査/` `SNS運用/note/` `SNS運用/archive/note/`）、自動検証5項目
- **登録先**: [[Claudian-スキル一覧.md]] の §2-2 に追記済み

---

## ⚪ 見送り

> 候補に上がったが「これはスキル化しない」と判断したもの。理由付きで残す（同じ判断を繰り返さないため）

> ※ 現在エントリなし

---

## 📝 候補エントリのフォーマット

新しい候補を追加するときは以下の形式で書く：

```markdown
### {スキル候補名（仮）}

- **検出日時**: YYYY-MM-DD HH:MM（最新の検出。1回目の日付も保持）
- **検出回数**: N
- **トリガー条件**: ユーザーが「〜」と言ったとき / 〜のパターンが現れたとき
- **典型例**:
  - YYYY-MM-DD: {そのときの会話・タスクの要約}
  - YYYY-MM-DD: {〃}
- **実装案（ドラフト）**:
  - 入力: 〜
  - 処理: 〜
  - 出力: 〜
- **既存スキルとの関係**: {重複するか / 連携するか / 完全新規か}
- **メモ**: {実装するか迷う理由・判断軸}
```

## 🧭 候補追加の判断軸

以下のいずれかを満たすパターンが現れたら候補に追加：

1. **頻度**: 同じ作業（命名規則・配置先・処理フロー）が直近1ヶ月で3回以上
2. **明示的依頼**: ユーザーが「これスキルにしたい」と言った
3. **作業量**: 毎回30秒以上かかる手作業がある
4. **再現性**: 入力 → 出力のマッピングが安定して書ける

逆に**候補にしないケース**:

- 1回限りの特殊作業
- ユーザーの判断が毎回変わる（自動化できない）
- 既存スキルの軽い拡張で済む（→ 既存スキルを更新）

## 🔁 ライフサイクル

```
パターン検出 → 🟡観察中 → 3回到達 → 🟢候補確定
                                      ↓
                       ユーザー承認 → 🔵実装決定 → 実装 → ✅実装済み
                       ユーザー却下 → ⚪見送り
```

## 🔗 関連

- `/reflect` — このファイルに自動追記するスキル（次セッション以降）
- [[Claudian-スキル一覧.md]] — 実装済みスキルの正式カタログ
- [[CLAUDE.md]] — 自己改善ループのルール根拠
- [[_ memory/long-term.md]] — 自己改善ループは行動原則として明記
